{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd app && npm install && npm run build --workspace=web",
  "outputDirectory": "app/apps/web/.next"
}
